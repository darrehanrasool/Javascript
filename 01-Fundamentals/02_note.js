// const job; this will throw an error
// As const variable can not be empty
window.alert("Dar Rehan Rasool");
alert("Dar Rehan Rasool");
console.log("it will log the string to the output console");
// we can reassign the value, block scoped.
let variable = 45;
// we can't reassign the value, block scoped.
const dob = 2000;
// we can reassign the value, funtion scoped.
var demo = "Rehan";
// conditionals
if (variable === 45) alert("hi buddy");
//prompt() is a inbuilt funtion in javacsript that's used to take input from user return type string.
let profile_name = prompt("Enter the name buddy \n");
console.log(profile_name);
// in javascript we use typeof() funtion to get type of data type return type string.
console.log(typeof country);
console.log(typeof district);
console.log(typeof population);
console.log(typeof language);
console.log(typeof isIsland);
// it will trigger number and string based on the operation performed on operands.
console.log("9" - "5"); // -> 4
console.log("19" - "13" + "17"); // -> '617'
console.log("19" - "13" + 17); // -> 23
console.log("123" < 57); // -> false
console.log(5 + 6 + "4" + 9 - 4 - 2); // -> 1143
